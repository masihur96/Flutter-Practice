import 'package:flutter/material.dart';

class Fruts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Fruts",style: TextStyle(
          fontSize: 50,
        ),),
      ),
    );
  }
}
